import React from "react";
import RegisterForm from "../Autenticacion/RegisterForm"; // ✅ Importamos el formulario correctamente

export const Register: React.FC = () => {
  return (
    <div>
      <RegisterForm />
    </div>
  );
};

export default Register;
